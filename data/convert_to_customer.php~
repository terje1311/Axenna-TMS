<?php
session_start();
if ($_SESSION['userID']!="") {  // User logged in

require_once "db.php";
require_once "../lang/no_nb.php";

$regNumber = $_GET['regNumber'];

$query = "UPDATE ".$companies." SET companyStatus='customer' WHERE regNumber=".$regNumber;
if (!$Result= mysql_db_query($DBName, $query, $Link)) {
           	print "No database connection <br>".mysql_error();
        } else {
				print $s_customer_deleted;
		  }

} else { // user not logged in
?>
<script language="Javascript">
document.location = "../login.php";
</script>
<?php
} 
?>